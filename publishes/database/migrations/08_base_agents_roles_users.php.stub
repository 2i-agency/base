<?php

use Illuminate\Database\Schema\Blueprint;
use Illuminate\Database\Migrations\Migration;
use Chunker\Base\Libs\Columns;

/**
 * Миграция для таблицы связей представителя с ролью или пользователем
 */
class CreateBaseAgentsRolesUsers extends Migration
{
	protected $table = 'base_agents_roles_users';


	public function up() {
		Schema::create($this->table, function(Blueprint $table) {

			$table->engine = 'MyISAM';
			$table->comment = 'Полиморфическая связь представителя с ролью или пользователем';

			/** Ключ агента */
			$table
				->integer('agent_id')
				->unsigned()
				->index()
				->comment('Ключ агента');

			/** Ключ связанной модели */
			$table
				->integer('model_id')
				->unsigned()
				->index()
				->comment('Ключ связанной модели');

			/** Тип связанной модели */
			$table
				->string('model_type')
				->index()
				->comment('Тип связанной модели');

		});
	}


	public function down() {
		Schema::drop($this->table);
	}
}