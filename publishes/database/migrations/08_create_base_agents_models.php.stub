<?php

use Illuminate\Database\Schema\Blueprint;
use Illuminate\Database\Migrations\Migration;
use Chunker\Base\Libs\Columns;

/**
 * Миграция для таблицы связей моделей с представителем (ролью или пользователем)
 */
class CreateBaseAgentsModels extends Migration
{
	protected $table = 'base_agents_models';


	public function up() {
		Schema::create($this->table, function(Blueprint $table) {

			$table->comment = 'Полиморфическая связь моделей с представителем (ролью или пользователем)';

			/** Ключ связанного пользователя или роли */
			$table
				->integer('agent_id')
				->unsigned()
				->index()
				->comment('Ключ связанного пользователя или роли');

			/** Ключ связанной модели */
			$table
				->integer('model_id')
				->unsigned()
				->index()
				->comment('Ключ связанной модели');

			/** Тип связанной модели */
			$table
				->string('model_type')
				->index()
				->comment('Тип связанной модели');

		});
	}


	public function down() {
		Schema::drop($this->table);
	}
}